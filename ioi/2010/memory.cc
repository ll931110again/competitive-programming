int value[55];
bool vis[55];

void play() {
    int maxc = 50;
    while (true) {
        int u = -1;
        for (int i = 1; i <= maxc; i++) if (!vis[i]) {
            u = i;
            break;
        }

        if (u < 0) {
            break;
        }

        for (int v = u + 1; v <= maxc; v++) if (!vis[j]) {
            
        }
    }
}