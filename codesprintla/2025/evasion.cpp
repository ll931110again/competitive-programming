// F – Evasion (stub)
// https://open.kattis.com/problems/evasion

#include <bits/stdc++.h>
using namespace std;

int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  int n, m, s, t, p;
  if (!(cin >> n >> m >> s >> t >> p))
    return 0;
  while (m--) {
    int u, v, w;
    cin >> u >> v >> w;
  }
  cout << -1 << '\n';
  return 0;
}
